let divisor = 3
let divident = 20

let quotient = (divident / divisor).toFixed(2)
let remainder = divident % divisor
console.log(divident, "is divided by", divisor, "which lead to quotient", quotient, "with remainder", remainder);
